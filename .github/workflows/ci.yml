name: CI

on: [push]

jobs:
  tests:
    name: "Rental NFTs for Real Estate – Syntax & Deployment Plan Check"
    runs-on: ubuntu-latest

    steps:  
      - name: "Checkout repository"  
        uses: actions/checkout@v4  

      - name: "Validate Clarinet contracts and deployment plan"  
        uses: docker://hirosystems/clarinet:latest  
        with:  
          args: check --deployment-plan-path deployments/default.simnet-plan.yaml
